{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "test": "tsc -p . && node --test 'client/test/*.test.mjs'",
    "test:browser": "tsc -p . && node client/test/browser/run.mjs"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3"
  },
  "devDependencies": {
    "puppeteer-core": "^25.12.0",
    "typescript": "^7.0.2"
  }
}
